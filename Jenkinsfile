@Library('jenkins-scripts-library') _
// JDK6 is configured to be JDK11. We don't have permissions to add JDK labels so we used the old one
buildMavenPipeline(agent: 'IT-CIM-MDAA-DataCollection-Linux', jdk: 'JDK6', mvnJvmFlgs: '-Poracle')
